import { ValidateWebDirective } from './validate-web.directive';

describe('ValidateWebDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateWebDirective();
    expect(directive).toBeTruthy();
  });
});
