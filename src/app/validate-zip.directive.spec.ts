import { ValidateZipDirective } from './validate-zip.directive';

describe('ValidateZipDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateZipDirective();
    expect(directive).toBeTruthy();
  });
});
